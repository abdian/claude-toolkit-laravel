# تغییرات

فرمت بر اساس [Keep a Changelog](https://keepachangelog.com/fa/1.1.0/).

## تغییرات v1.8-laravel — لایهٔ امنیت

امنیت در یک دستور جا نمی‌شود، چون وقتی یادت می‌افتد صدایش بزنی، لحظه‌اش گذشته.
در سه نقطه نشست:

- **`/security-review` (تازه)** — ممیزی امنیتی مخصوص لاراول. به ترتیبِ جایی
  می‌گردد که اپ‌های لاراولی واقعاً می‌شکنند: Policy جاافتاده، IDOR از راه route
  model binding، mass assignment به‌عنوان ارتقای دسترسی، injection در جایی که
  از فریم‌ورک دور زده شده، آپلود، سشن، نشت داده، و امضای وریفای‌نشدهٔ وبهوک.

  انضباط مرکزی‌اش: **هر finding باید بگوید چه کسی و از چه درخواستی به آن
  می‌رسد.** مسیر قابل‌دسترس نداشته باشد، finding نیست و در HARDENING می‌نشیند.
  خروجی با قالب چک‌باکسی `/fix-one` سازگار است.

- **بخش «Trust boundaries» در `CLAUDE.laravel.md.template`** — تنها لایهٔ
  همیشه‌فعال. چهار سؤالی که قبل از نوشتن کدِ مرزی باید در پاسخ بیایند، و
  قوانین قطعی: `$request->all()` به مدل نه، `$guarded = []` روی مدل نقش‌دار
  ممنوع، مجوز روی رکورد نه روی id، `hash_equals` برای مقایسهٔ سکرت، وریفای
  امضای وبهوک قبل از خواندن بدنه، `timeout()` روی هر `Http::`.

  با یک قانون که بر نردبان اولویت دارد: **نردبان کد را کوچک می‌کند، مرز را
  نازک نمی‌کند.** حتی زیر `/lazy ultra`.

- **قدم ۶ در `/plan-check`** — قدمی که از مرز اعتماد رد می‌شود باید از قبل سه
  چیز را نام ببرد: چه کسی اجازه دارد، چه چیزی شکل ورودی را تأیید می‌کند، چه
  شکلی خارج می‌شود. نام نبرد، محتمل‌ترین باگ امنیتی پلن همان است.

- **دستهٔ «Someone doing it on purpose» در `/edges`** — نه کاربر بی‌دقت، آنکه
  دارد می‌کاود: عوض کردن id در URL، پست کردن فیلدی که فرم نشان نداده، replay
  وبهوک، آپلودی که پسوند و محتوایش نمی‌خواند. این دسته به‌ندرت IGNORE می‌شود و
  دلیلش باید یک کنترل موجود باشد، نه «بعید است».

- مجموع skillها: **۱۸ تا**.

## تغییرات v1.7.1-laravel

- **`/explain`** — توضیح می‌دهد چه تغییر کرد و چرا، در یک گزارش HTML خودبسنده.
  دامنهٔ پیش‌فرض کارِ کامیت‌نشده است؛ یک کامیت، یک بازه، یا `--project` هم
  می‌گیرد. گزارش کامیت با نام موضوع کامیت ذخیره می‌شود.

  مهم‌ترین بخشش **ممیزی دامنه** است: هر فایل تغییرکرده باید به چیزی که خواستش
  برگردد — خطی در `BRIEF.md`، یک finding شماره‌دار، سطری در `specs/`، یا پیام
  کامیت. هرچه ردیابی نشود `UNTRACED` گزارش می‌شود، با قالب چک‌باکسی که
  `/fix-one` می‌تواند بگیردش. اسکیل صریحاً منع شده از نوشتن دلیل باورپذیر
  برای تغییری که دلیلی نداشته.

  این تنها چک **مکانیکیِ** دوم مجموعه است بعد از قدم ۵ `/fix-one` — بقیهٔ
  دفاع در برابر کد اضافه به دنبال کردن دستورالعمل توسط مدل تکیه دارد.

- مجموع skillها: **۱۷ تا**. `/explain` در دستهٔ `--all` است، نه استارتر.

## تغییرات v1.7-laravel

نسخه‌ی «جمع‌وجور کردن»: چیزی که در v1.6.1 نیمه‌کاره مانده بود تمام شد، و
مستندات با رفتار واقعی کد هم‌خوان شد.

**اضافه‌شده**

- **`/spec-import`** — یک سند brain-dump (ورد، markdown، یا متن پیست‌شده) را بین
  فایل‌های `specs/` پخش می‌کند. فقط چیزی که سند صریحاً گفته می‌نویسد و بقیه را
  در «فهرست شکاف‌ها» گزارش می‌دهد؛ جای خالی را حدس نمی‌زند. مجموع skillها
  رسید به **۱۶ تا**.
- **`docx2md.py`** — مبدل `.docx` به markdown با کتابخانه استاندارد پایتون،
  بدون نیاز به pandoc یا python-docx. جدول‌ها را هم درست درمی‌آورد. حالا یک
  نسخه‌اش **داخل خود `skills/spec-import/`** هم هست، پس هر طور که skill نصب
  شود (لینک، کپی، پروژه‌ای، سراسری) مبدلش را همراه دارد.
- **بخش `specs/` در README** — استارتر `specs/` از v1.6 همراه بسته بود ولی
  هیچ‌جای مستندات توضیح داده نشده بود. حالا ترتیب اجباری پر کردن، قرارداد
  `[...]` در برابر `[؟]`، و نقش هر فایل مستند شده.
- **بخش «همراهان اختیاری»** — Laravel Boost و OpenSpec با دستور نصب و مرز
  مسئولیت هرکدام. skillهایشان **عمداً اینجا کپی نشده‌اند**: هر دو ابزار
  skillهای خودشان را تولید و به‌روز می‌کنند و یک کپی منجمد بعد از اولین
  `composer update` دروغ می‌شود.

**اصلاح‌شده**

- **`install.sh` روی bash قدیمی می‌افتاد.** `for arg in "$@"` زیر `set -u`
  روی bash ۴.۳ و پایین‌تر — از جمله `/bin/bash` پیش‌فرض macOS که هنوز ۳.۲
  است — اجرای بدون آرگومان را با `$@: unbound variable` می‌شکست. شد
  `${1+"$@"}`.
- **`install.sh --help` ناقص چاپ می‌شد.** `sed -n '2,18p'` بلوک راهنما را
  وسط جمله‌ی هشدارِ «skill شخصی، skill پروژه‌ای را کنار می‌زند» قطع می‌کرد —
  دقیقاً همان هشداری که بیشترین اهمیت را دارد. شد `2,20p`.
- **پیام خطای symlink مسیر اشتباه می‌داد** — `_toolkit/skills/` را
  سخت‌کد کرده بود؛ حالا مسیر واقعی نصب را چاپ می‌کند.
- **`/spec-import` مسیر مبدل را سخت‌کد کرده بود** (`_toolkit/docx2md.py`) و
  اگر پوشه اسم دیگری داشت می‌شکست. حالا سه محل استاندارد را می‌گردد.
- **بخش «نصب» README هنوز v1.6 را توصیف می‌کرد** و با `install.sh` تناقض
  داشت: از `--link` می‌گفت که دیگر پیش‌فرض است، از نصب سراسری می‌گفت که دیگر
  پیش‌فرض نیست، و «پنج ابزار پایه» را می‌شمرد در حالی که از v1.6 شش‌تاست.
  کامل بازنویسی شد.
- **نقشه فایل‌ها (بخش ۳)** نصب سراسری را نشان می‌داد و `spec-import` در آن
  نبود. با ساختار پروژه‌ای بازنویسی شد.

## تغییرات v1.6

ایده‌های خوبِ [ponytail](https://github.com/DietrichGebert/ponytail) بدون خودِ
پلاگین — سازگار با ساختار همین جعبه‌ابزار، و لاراول‌محور.

- **نردبان ۷ پله‌ای** توی هر دو تمپلیت (`## The ladder`). قبل از نوشتن هر کد،
  روی اولین پله‌ای که جواب می‌ده وایسا: لازمه اصلاً؟ → تو کدبیس هست؟ →
  `Str`/`Arr`/`Collection`/`Number` داره؟ → فیچر فریم‌ورک/دیتابیس داره؟ →
  پکیج نصب‌شده داره؟ → یک‌خط می‌شه؟ → تازه بعدش کد. نسخه لاراولی پله‌ها
  مشخصاً می‌گه constraint دیتابیس بهتر از چک اپلیکیشنی، `casts` بهتر از
  accessor+mutator، `Gate::before` بهتر از چک نقش تو ده جا.
  **نردبان بعد از فهمیدن مسئله اجرا می‌شه، نه به‌جاش** — دیف کوچیک تو جای
  اشتباه، تنبلی نیست، باگ دومه.
- **`/lazy`** — سطح سخت‌گیری (`lite`/`full`/`ultra`/`off`) رو توی `CLAUDE.md`
  می‌نویسه، پس بین سشن‌ها می‌مونه.
- **`/debt`** — مارکرهای `lazy:` رو جمع می‌کنه توی `DEBT.md` و چک می‌کنه شرط
  ارتقای هرکدوم فعال شده یا نه. با فرمت چک‌باکس `AUDIT.md`، پس
  `/fix-one DEBT.md n` تسویه‌شون می‌کنه و `/next` می‌شمارتشون.
- **بخش «صرفه‌جویی کامنت و خروجی»** توی هر دو تمپلیت — کامنت فقط WHY نه WHAT،
  بدون docblock تکراری روی متدی که امضاش خودش گویاست، بدون بنر بخش‌بندی،
  بدون کد کامنت‌شده، بدون روایت دیف تو کامنت؛ سقف: حداکثر یک کامنت برای هر
  بلوک غیربدیهی. در جواب به کاربر هم: اول کد، بعد حداکثر سه خط. اگه توضیح از
  خود کد بلندتر شد، توضیح رو پاک کن. **استثنا:** گزارش یا واکاوی‌ای که خودت
  خواستی، کامل داده می‌شه — این قانون فقط جلوی وراجیِ نخواسته رو می‌گیره.
- **`/find-overengineering`** دو سیگنال جدید گرفت: شماره ۱۱ نویز کامنت (نسبت
  خط کامنت به خط کد، بدترین ۵ فایل) و شماره ۱۲ شمارش مارکرهای `lazy:` — با
  این قید صریح که قضاوت درباره‌شون کارِ `/debt`ه، نه اینجا.
- **`/next`** حالا `DEBT.md` رو هم می‌شماره.
- **`install.sh`** — `lazy` رفت تو مجموعه استارتر، `debt` تو بقیه (۱۵ تا شد).

## تغییرات v1.5-laravel

- **`templates/CLAUDE.laravel.md.template` جدید** — نسخه مخصوص لاراول: دستورات
  artisan/Pest/Pint/Larastan، سیاست مایگریشن (مایگریشن merge‌شده فریزه)،
  کانونشن‌های لاراول (FormRequest، Policy، API Resource، صف Redis)، قوانین
  دیتابیس-به-ازای-هر-سرویس و ارجاع بین‌سرویسی فقط با UUID. پیش‌فرض استک:
  MariaDB (جایگزین: MySQL/PostgreSQL به انتخاب هر سرویس) + Redis برای
  کش/صف — همه قابل تغییر، توی خود تمپلیت علامت‌گذاری شده.
- **مجوز ابزارهای PHP** — `fix-one` و `audit-codebase` حالا
  `Bash(php *)`، `Bash(composer *)` و `Bash(vendor/bin/*)` دارن؛ قبلاً روی
  ریپوی لاراول نمی‌تونستن `php artisan test` یا Pint/PHPStan اجرا کنن.

## تغییرات v1.4

- **`tutorial.html` جدید** — نسخه تعاملی آموزش با همون زبان طراحی guide:
  دیالوگ‌های حبابی (تو / Claude)، نمونه فایل‌های تاشو، FAQ آکاردئونی،
  نوار پیشرفت خواندن، و دکمه کپی روی پرامپت‌ها. guide و README بهش لینک شدن.

## تغییرات v1.3

- **`TUTORIAL.md` جدید** — آموزش قدم‌به‌قدم برای کسی که از صفر شروع می‌کنه:
  پنج مفهوم به زبان ساده، سه پروژه کامل (Tier 1 تا 3) با دیالوگ واقعی و
  نمونه خروجی هر فایل، جدول «الان چی بزنم؟»، هشت اشتباه رایج، FAQ و برگه
  تقلب. README مرجع می‌مونه؛ TUTORIAL نقطه شروعه.

## تغییرات v1.2

- **قانون کامیت** به `templates/CLAUDE.md.template` اضافه شد: کامیت فقط با
  دستور صریح کاربر، استثنای `/fix-one`، و ممنوعیت هر اشاره‌ای به Claude/AI
  در کامیت‌ها.
- **`templates/settings.json.template` جدید** — لایه اجرای قطعی همون قانون:
  `attribution` خالی (حذف Co-Authored-By و footer) + `permissions.ask` روی
  `git commit` (تأیید قبل از هر کامیت). مستنداتش تو بخش نصب.

## تغییرات v1.1

- **`/plan-check` اضافه شد** — داوری پلن در سشن تازه، قبل از پیاده‌سازی.
  عضو پنج‌تایی پایه. ارزون‌ترین نقطه گرفتن over-engineering.
- **فرمت واحد finding** — `- [ ] #n SEVERITY file:line — …` در `AUDIT.md` و
  `COMPLEXITY.md`. شمارنده `/next` و تیک `/fix-one` حالا واقعاً به هم وصلن.
- **اصلاح `allowed-tools`** — `fix-one` حالا `Edit/Write` داره (قبلاً اجازه
  تغییر هیچ فایلی رو نداشت!)؛ `audit-codebase` و `find-overengineering` هم
  `Write` گرفتن تا بتونن گزارششون رو بنویسن.
- **`ctx-audit`** — `agent: Explore` حذف شد (اون ایجنت read-only بود و
  نمی‌تونست `INVENTORY.md` بنویسه). حالا کاوش با ساب‌ایجنت‌های Explore انجام
  می‌شه ولی نوشتن فایل با خود سشن.
- **پله staleness در `/next`** — `DECISIONS.md` جدیدتر از فایل‌های تولیدشده
  باشه → خودش `/ctx-generate` رو پیشنهاد می‌ده.
- **فاز صفر در `ctx-verify`** — lint استاتیک ارزون (بودجه خطوط، گلوب‌های
  بی‌کوتیشن، referenceهای گمشده، تناقض‌ها) قبل از پروب‌های گرون.
- **برش عمودی اول** — `/start` برای Tier 2/3 «اولین milestone = یک slice نازک
  end-to-end» رو در `PROJECT.md` ثبت می‌کنه، به‌علاوه بخش Non-goals سطح محصول.
- **ریزه‌کاری‌ها** — شمارنده findings مقاوم شد (`grep -h | wc -l`)، خط `total`
  از لیست بزرگ‌ترین فایل‌ها فیلتر شد، نکته ویندوز (Git Bash/WSL) به نصب اضافه شد.
